server:
  port: 8848

spring:
  application:
    name: eureka


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:${server.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0
