server:
  port: 8848

spring:
  application:
    name: eureka

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://register-server:8848/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0
